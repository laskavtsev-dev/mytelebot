# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: laskavtsev
  tag: "v1.0.5-0898db2-arm64"
  arch: amd64
# Додатково визначте секцію для токену TELE_TOKEN
secret:
  name: "mytelebot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true
